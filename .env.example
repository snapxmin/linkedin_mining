# Optional Serper search provider key for live public-index discovery.
# Leave unset to use the built-in demo provider and CSV workflow.
SERPER_API_KEY=

# SQLite database path used by the default application instance.
DATABASE_PATH=talent.db
